// pch
#include "PreCompiled.h"

#include "Particle.h"

Voxel::UI::Particle::Particle()
	: startColor(0.0f)
	, endColor(0.0f)
	, speed(0.0f)
	, accelRad(0.0f)
	, accelTan(0.0f)
	, lifeSpan(0.0f)
	, livedTime(0.0f)
	, startSize(0.0f)
	, endSize(0.0f)
	, startAngle(0.0f)
	, endAngle(0.0f)
{}
